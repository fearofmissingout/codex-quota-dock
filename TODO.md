# TODO

## macOS Native App

- Build a native macOS edition with SwiftUI/AppKit instead of trying to make the Fyne UI feel fully native on macOS.
- Keep the existing app data format compatible where practical: profiles, stored auth JSON files, backups, and auth switch history.
- Start with a focused MVP: menu bar app, quota popover, profile settings, auth switching, and Codex restart.
- Defer local usage charts until the native shell is stable.
- Revisit whether to split the macOS app into a separate GitHub repository after the native MVP has real user feedback.
