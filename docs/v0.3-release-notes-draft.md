# v0.3.0 Release Notes Draft

This draft is intended for the GitHub release after manual preview testing passes.

## Highlights

- Added configurable low-quota alerts for monitored profiles.
- Added lower-size release builds with stripped metadata.
- Added safer release builds that disable Go VCS stamping.
- Improved Windows local build scripts so failed builds do not report success.
- Improved CI so only the newest branch build remains active.
- Changed macOS amd64 artifact generation to avoid long Intel runner queues.

## Low-Quota Alerts

- Configure alerts as `Off`, `5%`, `10%`, `20%`, or `30%`.
- Alerts are evaluated after a successful quota refresh.
- The app sends one system notification per profile and quota window while usage remains below the threshold.
- Duplicate notifications are suppressed until the quota recovers above the threshold.

## Build and Artifact Changes

- Release builds use `-buildvcs=false`, `-trimpath`, and stripped linker flags.
- Windows GUI builds continue to use `-H=windowsgui`, so the app opens without a terminal window.
- Windows local scripts now resolve `gcc` and pass its location through `CC` and `COMPILER_PATH` for more reliable CGO builds.
- PowerShell local builds now fail immediately if `go test`, `go build`, or `strip` fails.

## Size Comparison

Measured from the v0.3 preview CI artifacts:

| Artifact | v0.2.0 | v0.3 preview | Change |
| --- | ---: | ---: | ---: |
| Windows exe | 50.84 MB | 24.38 MB | -52.1% |
| Windows zip | 17.77 MB | 11.57 MB | -34.9% |

## Preview Artifacts

The preview artifacts used for verification were built by GitHub Actions run `26705726114`.

- `codex-quota-dock-windows-amd64.zip`
- `codex-quota-dock-linux-amd64.zip`
- `codex-quota-dock-macos-amd64.zip`
- `codex-quota-dock-macos-arm64.zip`

## Verification

- Windows, Linux, macOS amd64, and macOS arm64 CI builds completed successfully.
- `CGO_ENABLED=0 go test ./...` passes locally.
- `go version -m` confirms v0.3 binaries include `-trimpath=true` and do not include `vcs.revision`.
- A sensitive-string scan was run against committed files; only test fixtures contain fake token strings.

## Before Publishing

- Complete the manual checks in `docs/v0.3-test-checklist.md`.
- Confirm profile switching, restart prompts, alert settings, usage charts, and floating-window behavior on the Windows preview.
- Create the final GitHub release only after preview testing is accepted.
